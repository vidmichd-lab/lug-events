# Сайт-афиша событий 🎭

Современное веб-приложение для просмотра и управления событиями по городам с красивым дизайном, возможностью регистрации пользователей, сохранения в избранное и подписки на места.

## ✨ Особенности

- 🎨 Современный и красивый дизайн с градиентами и анимациями
- 📱 Полностью адаптивный интерфейс
- 🔐 Безопасная аутентификация через JWT
- ⭐ Сохранение событий в избранное
- 🔔 Подписка на места для получения уведомлений
- 🛡️ Система жалоб на события
- 👨‍💼 Админ-панель для управления контентом
- 🚀 Готово к развертыванию в Yandex Cloud

## Архитектура

### Backend
- **Технологии**: Node.js, Express.js, PostgreSQL, JWT
- **API**: RESTful API
- **Аутентификация**: JWT токены
- **База данных**: PostgreSQL (Yandex Managed PostgreSQL)

### Frontend
- **Технологии**: Next.js 14, React, TypeScript, Tailwind CSS
- **Роутинг**: App Router
- **Состояние**: React Context + Local Storage
- **Дизайн**: Современный UI с градиентами, анимациями и адаптивной версткой

### Инфраструктура (Yandex Cloud)
- **База данных**: Yandex Managed Service for PostgreSQL
- **Backend**: Yandex Container Registry + Yandex Serverless Containers
- **Frontend**: Yandex Object Storage + Cloud CDN
- **Load Balancer**: Yandex Application Load Balancer

## Структура проекта

```
/
├── backend/          # Backend API
├── frontend/         # Next.js приложение
├── infrastructure/   # Конфигурация для Yandex Cloud
└── docker-compose.yml # Для локальной разработки
```

## Основные функции

### Пользовательские функции
- Регистрация и авторизация
- Просмотр событий по городам
- Фильтрация по категориям
- Сохранение событий в избранное
- Подписка на места (музеи, театры, кафе и т.д.)
- Жалоба на событие
- Поделиться ссылкой на событие

### Админ-панель
- Управление событиями (CRUD)
- Управление местами
- Управление категориями
- Управление пользователями
- Модерация жалоб

## Модели данных

### Event (Событие)
- id, title, description, startDate, endDate, averagePrice, link, organizer, categoryId, placeId, cityId, createdAt, updatedAt

### Place (Место)
- id, name, type (museum, theater, cafe, restaurant, cinema, space), address, cityId, description, createdAt, updatedAt

### User (Пользователь)
- id, email, password (hashed), name, role (user, admin), createdAt, updatedAt

### Category (Категория)
- id, name, slug, createdAt, updatedAt

### City (Город)
- id, name, slug, createdAt, updatedAt

### Favorite (Избранное)
- id, userId, eventId, createdAt

### Subscription (Подписка)
- id, userId, placeId, createdAt

### Complaint (Жалоба)
- id, userId, eventId, reason, status (pending, reviewed, rejected), createdAt, updatedAt

## Быстрый старт

### 1. Клонирование репозитория
```bash
git clone https://github.com/vidmichd-lab/lug-events.git
cd lug-events
```

### 2. Локальная разработка
```bash
# Запуск всех сервисов
docker-compose up -d

# Выполнение миграций
docker-compose exec backend npm run migrate

# Заполнение тестовыми данными (опционально)
docker-compose exec backend npm run seed
```

### 3. Доступ к приложению
- Frontend: http://localhost:3000
- Backend API: http://localhost:3001
- Тестовый админ: `admin@example.com` / `admin123`

📖 **Подробные инструкции**: см. `QUICKSTART.md` и `NEXT_STEPS.md`

### Production (Yandex Cloud)
См. инструкции в `infrastructure/README.md` и `DEPLOYMENT.md`
