# Настройка GitHub репозитория

## Шаг 1: Создание репозитория на GitHub

1. Перейдите на [GitHub](https://github.com) и войдите в свой аккаунт
2. Нажмите кнопку **"New"** или **"+"** → **"New repository"**
3. Заполните форму:
   - **Repository name**: `events-platform` (или другое название)
   - **Description**: `Современный сайт-афиша событий с регистрацией, избранным и подписками`
   - **Visibility**: Public или Private (на ваше усмотрение)
   - **НЕ** добавляйте README, .gitignore или лицензию (они уже есть в проекте)
4. Нажмите **"Create repository"**

## Шаг 2: Инициализация Git в проекте

Откройте терминал в корневой директории проекта и выполните:

```bash
# Инициализация Git (если еще не сделано)
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: Events platform with modern design"

# Добавление remote репозитория (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/events-platform.git

# Переименование ветки в main (если нужно)
git branch -M main

# Отправка кода на GitHub
git push -u origin main
```

## Шаг 3: Настройка GitHub Secrets (для CI/CD)

Если вы хотите использовать GitHub Actions для автоматической сборки:

1. Перейдите в ваш репозиторий на GitHub
2. Откройте **Settings** → **Secrets and variables** → **Actions**
3. Добавьте следующие секреты (если нужно):
   - `NEXT_PUBLIC_API_URL` - URL вашего API
   - `DATABASE_URL` - строка подключения к БД (только для тестов)
   - `JWT_SECRET` - секретный ключ для JWT

## Шаг 4: Настройка веток

Рекомендуемая структура веток:

```bash
# Основная ветка для production
main (или master)

# Ветка для разработки
develop

# Ветки для фич
feature/название-фичи
```

Создание ветки develop:
```bash
git checkout -b develop
git push -u origin develop
```

## Шаг 5: Настройка защиты веток

1. Перейдите в **Settings** → **Branches**
2. Добавьте правило для ветки `main`:
   - ✅ Require a pull request before merging
   - ✅ Require approvals (1)
   - ✅ Require status checks to pass before merging
   - ✅ Require branches to be up to date before merging

## Шаг 6: Настройка GitHub Pages (опционально)

Если хотите автоматически деплоить документацию:

1. Перейдите в **Settings** → **Pages**
2. Source: **GitHub Actions**
3. Создайте файл `.github/workflows/docs.yml`:

```yaml
name: Deploy Docs

on:
  push:
    branches: [ main ]
    paths:
      - 'docs/**'
      - '*.md'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Pages
        uses: actions/configure-pages@v2
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v1
        with:
          path: '.'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v1
```

## Шаг 7: Добавление тегов для релизов

Для создания релизов:

```bash
# Создание тега
git tag -a v1.0.0 -m "Release version 1.0.0"

# Отправка тега на GitHub
git push origin v1.0.0
```

Затем на GitHub:
1. Перейдите в **Releases**
2. Нажмите **"Draft a new release"**
3. Выберите созданный тег
4. Добавьте описание релиза
5. Опубликуйте

## Шаг 8: Настройка Issues и Projects

1. Включите **Issues** в настройках репозитория
2. Создайте шаблоны для issues:
   - Bug report
   - Feature request
   - Question

3. Создайте Project board для отслеживания задач

## Шаг 9: Добавление Collaborators (если нужно)

1. Перейдите в **Settings** → **Collaborators**
2. Добавьте пользователей по email или username
3. Назначьте права доступа

## Шаг 10: Настройка Webhooks (для автоматического деплоя)

Если используете автоматический деплой:

1. Перейдите в **Settings** → **Webhooks**
2. Добавьте webhook:
   - **Payload URL**: URL вашего CI/CD сервера
   - **Content type**: application/json
   - **Events**: Push, Pull request

## Полезные команды Git

```bash
# Проверка статуса
git status

# Просмотр изменений
git diff

# Добавление изменений
git add .
git add конкретный-файл

# Коммит
git commit -m "Описание изменений"

# Отправка на GitHub
git push

# Получение обновлений
git pull

# Создание новой ветки
git checkout -b feature/новая-фича

# Переключение между ветками
git checkout main
git checkout develop

# Просмотр истории
git log --oneline --graph

# Отмена изменений
git restore файл
git restore --staged файл
```

## Структура коммитов

Рекомендуется использовать конвенциональные коммиты:

```
feat: добавлена функция избранного
fix: исправлена ошибка в фильтрах
docs: обновлена документация
style: улучшен дизайн карточек
refactor: рефакторинг API роутов
test: добавлены тесты
chore: обновлены зависимости
```

## Что дальше?

После настройки GitHub репозитория:

1. ✅ Код загружен на GitHub
2. 🔄 Настройте CI/CD (GitHub Actions уже настроен)
3. 🚀 Разверните на Yandex Cloud (см. DEPLOYMENT.md)
4. 📝 Настройте мониторинг
5. 🧪 Добавьте тесты
6. 📊 Настройте аналитику

## Полезные ссылки

- [GitHub Docs](https://docs.github.com)
- [GitHub Actions](https://docs.github.com/en/actions)
- [Conventional Commits](https://www.conventionalcommits.org)

